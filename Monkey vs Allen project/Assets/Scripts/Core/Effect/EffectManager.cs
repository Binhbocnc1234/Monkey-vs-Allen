using System.Collections.Generic;
using UnityEngine;

// public class EffectManager : MonoBehaviour{
//     private UpdateContainer
//     // private List<(IEntity, IEffect)> containerByEntity;
//     public override void AddElement(IEffect element) {
//         foreach(var effect in GetContainer()) {
//             if (effect.target == element.target && effect.)
//         }
//         base.AddElement(element);
//         // containerByEntity.Add((element.));
//     }
//     public List<IEffect> GetEffects(IEffectable e){
//         List<IEffect> res = new List<IEffect>();
//         foreach(IEffect effect in GetContainer()){
//             if (effect.target == e){
//                 res.Add(effect);
//             }
//         }
//         return res;
//     }
// }